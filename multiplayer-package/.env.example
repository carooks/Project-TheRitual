# Environment Variables Template

# Copy this file to .env in your project root and fill in your values

# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ⚠️ SECURITY WARNING ⚠️
# NEVER use the service_role key in browser code!
# Only use the 'anon' public key shown in the API settings
# The anon key is safe to expose in client-side code

# Optional: Hardware Bridge (if using hardware controllers)
# VITE_HARDWARE_BRIDGE_API=http://localhost:3001
